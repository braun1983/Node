const prompt=require('prompt-sync')();

var palavra = prompt('Digita a palavra:');

function calculaAnagrama(palavra){
    const quantLetras = palavra.lenght
    for let 
}


calculaAnagrama(palavra);